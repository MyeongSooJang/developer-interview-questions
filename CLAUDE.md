# CLAUDE.md

이 프로젝트는 Java 백엔드 개발자 면접 준비를 위한 질문/답변 저장소입니다.

## 목표

암기 후 면접에서 30초~1분 분량으로 말했을 때, 백엔드 면접관에게 "잘 아시네요" 반응을 끌어내는 수준의 답변을 작성합니다.

## 답변 작성 원칙

### 구조: 두괄식 3단락

모든 메인 질문의 답변은 아래 3단락 구조로 작성합니다.

```
① 정의     — 핵심 기술 키워드를 포함한 한 줄 정의
② 이유/원리 — 왜 필요한지, 어떻게 동작하는지, 구체적 예시
③ 실무 연결 — 실제 사용 방식, 연관 개념, 트레이드오프
```

### 키워드 처리

`### 키워드` 섹션을 별도로 만들지 않습니다. commit, rollback, ACID 같은 핵심 기술 용어는 ① 정의 단락 문장 안에 자연스럽게 녹입니다.

### 답변 길이

단락당 2~3문장, 전체 소리내어 읽었을 때 30초~1분 이내가 되도록 합니다.

### 문장 단위 작성

각 문장은 한 줄씩 끊어서 작성합니다. 문장을 한 줄에 몰아 쓰지 않습니다.

### 문장 연결

문장 간 연결은 면접에서 말할 때 자연스럽게 이어지는지를 기준으로 합니다.
단락 내 문장은 "이를 위해", "이 때문에", "반면", "따라서" 등 인과·대조 연결어를 활용해 논리적으로 이어지도록 합니다.
문장이 뚝 끊기거나 주제가 갑자기 전환되는 경우 연결어를 추가하거나 순서를 조정합니다.

### 꼬리질문

별도 `## Q.` 섹션으로 분리하지 않고, 메인 질문 아래 `### 꼬리질문` 리스트로 정리합니다.

꼬리질문의 답변은 3단락 구조를 강요하지 않습니다.
흐름 설명 중심으로 2~3문장으로 간결하게 작성합니다.
꼬리질문의 꼬리질문(2단계 이상)은 작성하지 않습니다.

메인 질문 답변을 작성할 때, 꼬리질문 리스트만 나열하고 끝내지 않습니다.
꼬리질문마다 답변까지 함께 작성해 바로 다음 리스트 항목으로 붙입니다 (`- 질문\n  답변` 형식).

## 파일 템플릿

```markdown
# 주제

## Q. 질문 내용

### 답변
[① 한 줄 정의 — 핵심 키워드를 자연스럽게 포함]

[② 이유/원리 — 왜 필요한지, 어떻게 동작하는지, 구체적 예시]

[③ 실무 연결 — 실제 사용 방식, 연관 개념, 트레이드오프]

### 꼬리질문
- 예상 꼬리질문 1
- 예상 꼬리질문 2
```

## 답변 작성 기준 (Claude에게)

새 질문을 추가하거나 기존 답변을 수정할 때 다음 기준을 따릅니다.

### 검증 (작성 전 필수)

답변을 작성하기 전에 반드시 웹 검색으로 사실을 확인합니다.

1. 공식 문서(Oracle, Spring 공식 문서 등) 또는 신뢰할 수 있는 기술 자료를 검색해 핵심 개념이 정확한지 확인
2. 검색 결과와 답변 내용이 일치할 때만 작성
3. 출처마다 설명이 다를 경우, 공식 문서 기준을 우선으로 함

### 문장 흐름 검증 (작성 후 필수)

답변 초안을 작성한 뒤, 저장하기 전에 반드시 소리내어 읽었을 때 어색한 부분이 없는지 검토합니다.

1. 번역체나 논문투 표현("능동적으로", "~에 기인한다" 등) 대신 면접에서 실제로 말하는 구어체 표현을 사용
2. 한 문장에 여러 개념을 욱여넣지 않고, 필요하면 문장을 나눔
3. 접속어로 이어지는 두 문장이 실제로 인과·대조 관계가 맞는지 확인 (어색하면 접속어를 바꾸거나 순서를 조정)
4. 신입/주니어 입장에서 쓰기 어색한 경험 주장("실무에서는 ~해왔습니다")은 피하고, 일반 지식으로 표현
5. 앞 단락에서 이미 말한 내용(사실·이유)을 뒤 단락에서 표현만 바꿔 반복하지 않았는지 확인 (②는 ①에 없는 이유·원리를, ③은 앞에 없는 실무 연결을 추가해야 함)

### 작성 기준

- **관점**: 백엔드 면접관이 듣고 싶어하는 포인트(동작 원리 + 실무 적용)를 중심으로 작성
- **키워드**: ① 정의에 핵심 기술 용어를 반드시 포함 (없으면 면접관이 "용어를 모르는 건가?" 오해할 수 있음)
- **예시**: ② 단락에 계좌이체, Spring Bean 등 구체적 실무 예시를 활용해 이해도를 보여줌
- **길이**: 답변을 소리내어 읽었을 때 30초~1분 안에 끝나는지 확인

## 디렉토리 구조

```
Java/           — Java 언어 관련 (JVM, GC, 컬렉션 등)
Spring/         — Spring Framework 관련
JPA/            — JPA/Hibernate 관련
DB/             — 데이터베이스 관련 (트랜잭션, 인덱스 등)
기본-CS/        — 네트워크, OS, 디자인 패턴 등
알고리즘-및-자료구조/
```

새 파일은 실제로 찾아본 면접 질문이 있을 때만 생성합니다.
